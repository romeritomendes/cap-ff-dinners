_schema-version: 3.3.0
ID: cap-ff-dinner
description: A simple CAP project.
version: 1.0.0
modules:
- name: cap-ff-dinner-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: cap-ff-dinner-auth
  - name: cap-ff-dinner-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: cap-ff-dinner-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: cap-ff-dinner-db
  parameters:
    buildpack: nodejs_buildpack
- name: cap-ff-dinner
  type: approuter.nodejs
  path: app/router
  requires:
  - name: cap-ff-dinner-repo-rt
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: cap-ff-dinner-auth
  - name: cap-ff-dinner-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: cap-ff-dinner-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: cap-ff-dinner-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - adminuser.zip
      name: adminuser
      target-path: resources/
- name: adminuser
  type: html5
  path: app/admin.user
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: cap-ff-dinner-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        redirect-uris:
        - https://*~{app-api/app-uri}/**
      tenant-mode: dedicated
      xsappname: cap-ff-dinner-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
  requires:
  - name: app-api
- name: cap-ff-dinner-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: cap-ff-dinner-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: cap-ff-dinner-html5-service
    service-plan: app-host
- name: cap-ff-dinner-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: cap-ff-dinner-destination-service
    service-plan: lite

- name: cap-ff-dinner-repo-rt
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime

parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    # - npm ci
    - cds build --production
